---
applyTo: '**'
---
# Project Rules — <REPO NAME>

Repo-specific rules. Shared instructions, skills, and prompts must NOT contradict
this file. If they do, this file wins.

## Error handling

<!-- e.g. "Throw <ProjectException> with HTTP status constant. Never create new
     exception classes." Or "Use Result<T,E> pattern". -->

## Dependency management

<!-- e.g. "All dependency versions live in <bom-module>/pom.xml. Never declare
     versions in child poms." -->

## Build / Test commands

<!-- e.g. "Full build: ./mvnw clean install -DskipTests" -->

## Module boundaries

<!-- e.g. "Controllers delegate to services; services own business logic;
     repositories are thin." -->

## Domain-specific patterns

<!-- e.g. "Two FooModel classes exist: DTO (no JPA) and entity (JPA). Open the
     correct one for the layer you're editing." -->

## Don'ts

<!-- Hard rules: things that must never appear in code review or generated code. -->
